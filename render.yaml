# OrbitFinder full-stack deployment blueprint for Render (Free-tier)

services:
  - type: web
    name: orbitfinder-frontend
    env: static
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    routes:
      - type: rewrite
        source: /(.*)
        destination: /index.html

  - type: web
    name: orbitfinder-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: node backend/index.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: orbitfinder-db
          property: connectionString
    autoDeploy: true

databases:
  - name: orbitfinder-db
    plan: free
